{{ 'section-energy-guidance.css' | asset_url | stylesheet_tag }}

<div class="energy-guidance{% if section.settings.full_width %} energy-guidance--full-width{% endif %}" style="
  --energy-bg-color: {{ section.settings.background_color }};
  --energy-title-color: {{ section.settings.title_color }};
  --energy-border-color: {{ section.settings.border_color }};
  --energy-button-bg: {{ section.settings.button_bg_color }};
  --energy-button-text: {{ section.settings.button_text_color }};
">
  <div class="page-width">
    {% if section.settings.title != blank %}
      <h2 class="energy-guidance__title h1" style="text-align: {{ section.settings.title_alignment_desktop }};">{{ section.settings.title }}</h2>
    {% endif %}
    
    <div class="energy-guidance__container">
      <div class="energy-guidance__grid">
        {%- for block in section.blocks -%}
          <div class="energy-guidance__item" {{ block.shopify_attributes }}>
            {% if block.settings.link != blank %}<a href="{{ block.settings.link }}" class="energy-guidance__link">{% endif %}
              <div class="energy-guidance__icon-wrapper">
                {%- if block.settings.image != blank -%}
                  {{ block.settings.image | image_url: width: 120 | image_tag:
                    loading: 'lazy',
                    width: 120,
                    height: 120,
                    class: 'energy-guidance__icon'
                  }}
                {%- endif -%}
              </div>
              {%- if block.settings.title != blank -%}
                <div class="energy-guidance__text">{{ block.settings.title }}</div>
              {%- endif -%}
            {% if block.settings.link != blank %}</a>{% endif %}
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

<style>
.energy-guidance {
  padding: 3rem 0;
  background: var(--energy-bg-color, #2B2B2B);
}

.energy-guidance__title {
  color: var(--energy-title-color, #fff);
  margin-bottom: 3rem;
  font-size: 2rem;
}

.energy-guidance__container {
  position: relative;
  margin: 0 -1.5rem;
  padding: 0 1.5rem;
}

.energy-guidance__grid {
  display: grid;
  grid-template-columns: repeat(var(--items-per-row, 8), 1fr);
  gap: 2rem;
  justify-items: center;
}

.energy-guidance__item {
  text-align: center;
}

.energy-guidance__link {
  text-decoration: none;
  color: inherit;
  display: block;
}

.energy-guidance__icon-wrapper {
  width: 120px;
  height: 120px;
  margin: 0 auto 1rem;
  border: 1px solid var(--energy-border-color, rgba(255, 255, 255, 0.2));
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: transform 0.3s ease;
}

.energy-guidance__item:hover .energy-guidance__icon-wrapper {
  transform: translateY(-5px);
}

.energy-guidance__icon {
  width: 60%;
  height: 60%;
  object-fit: contain;
}

.energy-guidance__text {
  background: var(--energy-button-bg, #fff);
  color: var(--energy-button-text, #000);
  padding: 0.5rem 1.5rem;
  border-radius: 50px;
  display: inline-block;
  font-size: 0.9rem;
  line-height: 1.2;
}

@media screen and (max-width: 749px) {
  .energy-guidance__title {
    text-align: {{ section.settings.title_alignment_mobile }} !important;
  }
  
  .energy-guidance__container {
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    scrollbar-width: none;
    -ms-overflow-style: none;
  }

  .energy-guidance__container::-webkit-scrollbar {
    display: none;
  }

  .energy-guidance__grid {
    display: flex;
    grid-template-columns: none;
    gap: 1.5rem;
    padding: 0.5rem 0;
    width: max-content;
  }

  .energy-guidance__item {
    flex: 0 0 auto;
    width: 120px;
  }

  .energy-guidance__icon-wrapper {
    width: 100px;
    height: 100px;
  }
}
</style>

{% schema %}
{
  "name": "能量指引",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "标题",
      "default": "ENERGY GUIDANCE"
    },
    {
      "type": "select",
      "id": "title_alignment_desktop",
      "label": "桌面端标题对齐方式",
      "options": [
        {
          "value": "left",
          "label": "靠左"
        },
        {
          "value": "center",
          "label": "居中"
        },
        {
          "value": "right",
          "label": "靠右"
        }
      ],
      "default": "left"
    },
    {
      "type": "select",
      "id": "title_alignment_mobile",
      "label": "移动端标题对齐方式",
      "options": [
        {
          "value": "left",
          "label": "靠左"
        },
        {
          "value": "center",
          "label": "居中"
        },
        {
          "value": "right",
          "label": "靠右"
        }
      ],
      "default": "center"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "背景颜色",
      "default": "#2B2B2B"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "标题颜色",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "border_color",
      "label": "图标边框颜色",
      "default": "rgba(255, 255, 255, 0.2)"
    },
    {
      "type": "color",
      "id": "button_bg_color",
      "label": "按钮背景颜色",
      "default": "#FFFFFF"
    },
    {
      "type": "color",
      "id": "button_text_color",
      "label": "按钮文字颜色",
      "default": "#000000"
    },
    {
      "type": "checkbox",
      "id": "full_width",
      "label": "全宽显示",
      "default": false
    }
  ],
  "blocks": [
    {
      "type": "guidance_item",
      "name": "能量项",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "图标"
        },
        {
          "type": "text",
          "id": "title",
          "label": "标题",
          "default": "能量"
        },
        {
          "type": "url",
          "id": "link",
          "label": "链接"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "能量指引",
      "blocks": [
        {
          "type": "guidance_item"
        },
        {
          "type": "guidance_item"
        },
        {
          "type": "guidance_item"
        },
        {
          "type": "guidance_item"
        }
      ]
    }
  ]
}
{% endschema %} 